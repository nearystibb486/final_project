#include <stdio.h>

int main() {
    int i;
    printf("Enter number of rows: ");
    scanf("%d", &i);

    if (i > 0) {
        printf("\n");
    }

    for (i = 1; i <= i; ++i) {
        printf("* ");
    }

    return 0;
}
